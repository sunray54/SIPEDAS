<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Izin Dasar -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Kamera (wajib) -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Penyimpanan (hanya untuk Android <=12) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".SipedasApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SIPEDAS"
        tools:targetApi="31">

        <!-- Launcher -->
        <activity
            android:name=".ui.MainContainerActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tanya Jawab Gejala -->
        <activity
            android:name=".ui.SymptomCheckActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Riwayat Diagnosis -->
        <activity
            android:name=".ui.HistoryActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Penjelasan Inferensi -->
        <activity
            android:name=".ui.DetailReasoningActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Profil Pengguna -->
        <activity
            android:name=".ui.ProfileActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Chat Pakar (LLM) -->
        <activity
            android:name=".ui.ChatPakarActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Langganan Premium -->
        <activity
            android:name=".ui.SubscriptionActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.LoginActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.RegisterActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Fitur Tambahan -->
        <activity android:name=".ui.forecast.ForecastActivity" />
        <activity android:name=".ui.iot.IoTSensorActivity" />
        <activity android:name=".ui.vr.VRActivity" />
        <activity android:name=".ui.research.ResearchActivity" />
        <activity android:name=".ui.community.CommunityActivity" />

    </application>
</manifest>